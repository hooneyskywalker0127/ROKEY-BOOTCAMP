스택, 큐, 덱

알고리즘 
ex)서울에서 대전 가는 **방법**

자료구조
데이터를 저장, 관리
세트, 스택, 큐, 덱, 리스트, 튜플, 딕셔너리

스택
데이터 삽입(push), 삭제(pop)가 top에서만 발생
후입선출(LIFO), BFS (너비 우선 탐색)
ex)프링글스

스택 구현 방식
1.리스트로
리스트.pop() : 마지막 삭제
리스트.append() : 마지막 추가 
리스트.pop(인덱스번호)
2.클래스로

큐
데이터 삽입(Enqueue), 삭제(Dequeue)
선입선출(FIFO)
ex)줄서기

리스트 구현 방식
1. 리스트
리스트.pop(0) : 처음삭제
리스트.append() : 마지막추가
2. 클래스

덱
스택 + 큐
양쪽 끝에서 삽입과 삭제가 가능한 자료구조
내장 모듈 collection
append : 덱의 마지막에 데이터를 삽입하는 동작
appendleft : 덱의 처음에 데이터를 삽입하는 동작
pop : 덱의 마지막 데이터를 제거하는 동작
popleft : 덱의 처음 데이터를 제거하는 동작


문자열을 '인수로 받아'~~~~ : 함수를 구현하라

set세트{}
중복을 허용하지 않는 자료구조
numbers.add(5)
numbers.remove(3)
