파일처리 함수들 자주 사용. 문법 반드시 암기

path 두가지
절대경로 : 최상위 폴더부터, 드라이브부터 모두 포함
상대경로 : 현재 작업 위치
주의 슬래시 방향
r 역슬래시
/ 문제 없음

파일 열기 모드 3가지 
r ead 읽기
w rite 쓰기   1. 파일 생성   2. 파일 수정(덮어쓰기)
a dd 추가        파일에 내용 추가

파이썬 기본 인코딩 = cp949

파일 여닫기 
객체 = open(path, 'mode', encoding)
객체.close()

파일 입출력
객체.write()     내용 추가
객체.read()

읽기 3종류
객체.read()	-내용 전체 문자열로 반환
객체.readline()	-결과값 반환
객체.readlines()    - 모든 줄을 읽고 각각 리스트형태로 반환

파일 자동으로 닫기
with open() as 객체:


파일 우클릭 해서 볼수 있음
절대경로(absolute path)
상대경로(relatibe path)

items()
딕셔너리(dictionary) 에서 쓰는 함수, “key와 value를 한 쌍씩 튜플 형태로 꺼내준다”
