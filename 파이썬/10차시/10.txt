키워드: 모듈, 상속

메인모듈
하위 모듈


멤버변수 : 클래스 변수, 인스턴스 변수

모듈= 파이썬 파일

import 파일이름

객체 = class명() = > 생성자 함수

import 모듈명
from 모듈명 import 함수 : 모듈로부터 함수를 import해주세요
from 모듈명 import* : 모든 내용 사용 (*모든, 전체라는 뜻)
(참고) import 모듈명 as 별칭

__name__ 내장 변수
메인 모듈의 경우, __main__ 이 저장됨
하위 모듈의 경우, 모듈명이 저장됨

random 모듈 자주 씀 = 암기

상속
class 이름(부모):
	코드블록
()는 대부분 함수이지만 부모클래스 함수는 예외


자식 클래스에서 부모의 메서드 호출할 때 사용
super = 부모클래스 ex) Animal
→ super().메서드명(인자)

오버라이딩 (상속에서 메소드 재정의)