그래프
DFS
BFS

그래프 : 관계를 표현하는 구조
노드(=정점, vertex)
엣지(=간선,연결선)

그래프를 딕셔너리로 표현
• 키(key): 노드
• 값(value): 키와 연결된 노드 리스트

DFS(깊이 우선 탐색)
스택 이용 구현
주로 모든 노드를 방문할때 사용

list() == []
extend()
reversed() 리스트의 요소 순서를 반대로 뒤집는 것
f스트링 안 변수는 {} 중괄호처리 ex)리스트, 튜플, 딕셔너리 다
f-string: 문자열 앞에 f를 붙이고, 중괄호 {} 안에 변수나 표현식을 넣으면 그 값이 문자열로 자동 변환되어 출력

12페이지 오타 3~5 가아니라 4~5

for 반복횟수 정해짐, 주로 시퀀스(리스트, 문자열, range 등)**를 돌면서 반복, 정확히 n번 반복
while 반복횟수 안정해짐, ~까지, 조건이 참(True)인 동안 계속 반복, 조건을 만족하는 동안, 즉 ~까지 반복, 횟수 미정

BFS(넓이 우선 탐색)
큐 이용 구현
주로 최단 경로, 임의의 경로를 찾을때


