리스트, 튜플, 딕셔너리 세개 다 중요(자료형)


리스트 자료형 가능, 그래서 자기도 가능, 순서 중요
*****인덱스는 숫자 0부터 시작

append() ()있으니 함수
함수(메소드)는 동작이나 기능을 수행을 한다


리스트 대괄호[]
변경 가능 (mutable) → 요소 추가, 삭제, 수정 가능
사용 할때 
데이터가 자주 바뀌는 경우
반복문 돌면서 새로운 값 추가

반환가능 = 리턴값이 있다 = 변수에 할당할 수 있다

튜플 소괄호(), 수정X, ex주민번호
데이터 생성 후 데이터 값이 변경되면 안되는 경우 유용하게 사용
괄호생략가능

immutable(불변객체)
한번 생성되면 값이 변경 불가능
값을 변경 시도하면 새로운 객체 생성, 기존 객체는 유지
ex)튜플, float str int

mutable(가변객체)
생성 후 내부값 변경 가능
객체를 수정하면 같은 주소를 가진 객체가 그대로 바뀐다
ex 리스트 dict set

딕셔너리 
중괄호{}
