swap 
1. temp 변수
2. a, b = b, a (튜플활용)
3. 함수를 이용



ex a= 1, b= 2
파이썬 말고 다른 언어에서는 둘이 값을 바꿀수 없어서 
temp 사용
파이썬은 가능

bound : 할당

defalut 인수가 우선순위 > 기본값
인수 없으면 defalut

default1.py 참고
1. 모든 매개변수에 기본값 설정 가능
2. 부분 매개변수에 기본값 설정시, 뒤에서부터 설정
3. 기본값이 있더라도 인수 전달 가능(전달된 인수의 우선 처리)



ex 26페이지

1번 함수 안 x 전역변수
2번 함수 안 x 지역변수

전역 변수 
함수 안에서 읽기 가능 / 쓰기 불가능

지역 변수 
함수 안에서 읽기 가능 / 쓰기 가능

지역 변수 읽기(read) 가능 /읽기 : 가져오기
전역 변수 지역 변수 쓰기(write) 불가능 /쓰기 : 수정

지역변수가 전역변수로 사용 global

타입 힌트 : 권장사항

재귀함수
기저 조건 ex if i ==0
재귀 단계 자기자신 호출

sum() :전달받은 반복 가능한 자료형(Iterable, 예: 리스트, 튜플, 딕셔너리의 값 등)의 모든 숫자 요소를 합산하여 그 결과를 반환
